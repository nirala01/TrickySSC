# robots.txt for trickyssc.com
User-agent: *
Allow: /

# Admin/utility pages are kept out of search via a "noindex" tag on each page
# (not blocked here, so Google can crawl them and honour the noindex).

Sitemap: https://trickyssc.com/sitemap.xml
